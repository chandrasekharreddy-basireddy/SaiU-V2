import {sortClasses} from './timetable.js';
export function ics(items,title='SaiU V2 Timetable'){const lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//SaiU V2//EN','CALSCALE:GREGORIAN','X-WR-CALNAME:'+title];const base=new Date();const dayIndex={Sunday:0,Monday:1,Tuesday:2,Wednesday:3,Thursday:4,Friday:5,Saturday:6};for(const c of sortClasses(items)){const d=new Date(base);const delta=(dayIndex[c.day]-d.getDay()+7)%7;d.setDate(d.getDate()+delta);const [sh,sm]=c.start.split(':').map(Number),[eh,em]=c.end.split(':').map(Number);const dt=(x)=>`${x.getFullYear()}${String(x.getMonth()+1).padStart(2,'0')}${String(x.getDate()).padStart(2,'0')}T${String(x.getHours()).padStart(2,'0')}${String(x.getMinutes()).padStart(2,'0')}00`;const s=new Date(d);s.setHours(sh,sm,0,0);const e=new Date(d);e.setHours(eh,em,0,0);lines.push('BEGIN:VEVENT',`UID:${c.id}@saiu-v2`,`DTSTART:${dt(s)}`,`DTEND:${dt(e)}`,`SUMMARY:${c.course}`,`LOCATION:${c.room}`,`DESCRIPTION:${c.code||''} — ${c.teacher||''}`,'END:VEVENT')}lines.push('END:VCALENDAR');return lines.join('\r\n')+'\r\n'}
export function downloadIcs(items){const blob=new Blob([ics(items)],{type:'text/calendar;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='saiu-timetable.ics';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
