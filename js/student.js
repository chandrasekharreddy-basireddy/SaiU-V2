const KEY='saiu-v2-academics';const defaults={attendanceTarget:75,attendance:{},exams:[]};export function academics(){try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...defaults}}}export function saveAcademics(v){localStorage.setItem(KEY,JSON.stringify({...academics(),...v}))}export function attendanceStatus(attended,total,target=academics().attendanceTarget){const pct=total?attended/total*100:0;return {pct,atRisk:pct<target,classesNeeded:pct>=target?0:Math.ceil((target*total-attended*100)/(100-target))}}export function addExam(exam){const a=academics();a.exams.push({...exam,id:crypto.randomUUID()});saveAcademics(a)}
