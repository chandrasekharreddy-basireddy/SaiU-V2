let registration=null;const scheduled=new Map();
const DAY_INDEX={Sunday:0,Monday:1,Tuesday:2,Wednesday:3,Thursday:4,Friday:5,Saturday:6};
export async function initNotifications(){if(!('serviceWorker' in navigator))return null;try{registration=await navigator.serviceWorker.ready;return registration}catch{return null}}
export async function notify(title,body){if(!registration||typeof Notification==='undefined'||Notification.permission!=='granted')return false;try{await registration.showNotification(title,{body,tag:'saiu-v2'});return true}catch{return false}}
export async function requestNotifications(){if(!('Notification' in window))return 'unsupported';return Notification.requestPermission()}
export function remindBeforeClass(c,minutes=10,now=new Date()){if(!c)return;const dayIndex=DAY_INDEX[c.day];if(dayIndex==null)return;const target=new Date(now);const delta=(dayIndex-target.getDay()+7)%7;target.setDate(target.getDate()+delta);const [h,m]=String(c.start).split(':').map(Number);if(!Number.isInteger(h)||!Number.isInteger(m)||h<0||h>23||m<0||m>59)return;target.setHours(h,m,0,0);const delay=target.getTime()-now.getTime()-minutes*60000;if(delay<=0||delay>=7*86400000)return;const key=`${c.id||`${c.day}-${c.start}-${c.course}`}:${target.getTime()}:${minutes}`;if(scheduled.has(key))return;const timer=setTimeout(()=>{scheduled.delete(key);void notify('Upcoming class',`${c.course} starts in ${minutes} minutes — ${c.room||'room TBD'}`)},delay);scheduled.set(key,timer)}
export function clearScheduledReminders(){for(const timer of scheduled.values())clearTimeout(timer);scheduled.clear()}
